{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "clean": "rm -rf node_modules dist .turbo .vercel",
    "db:generate": "bun run drizzle-kit generate",
    "db:migrate": "bun run drizzle-kit migrate",
    "db:push": "bun run drizzle-kit push",
    "db:studio": "bun run drizzle-kit studio"
  },
  "dependencies": {
    "@repo/core": "*",
    "@t3-oss/env-nextjs": "^0.13.10",
    "hono": "^4.12.2",
    "zod": "^4.3.6",
    "drizzle-orm": "^0.45.1",
    "pg": "^8.18.0",
    "better-auth": "^1.4.20"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bun": "^1.3.9",
    "@types/node": "^25.3.2",
    "drizzle-kit": "^0.31.9"
  }
}
